[package]
name = "gphotos-sync"
version = "2.0.0"
edition = "2024"
rust-version = "1.91"
build = "build.rs"
description = "Native Windows tray backup for Google Photos with content-aware deduplication"
license = "MIT"
readme = "README.md"
repository = "https://github.com/Henner4746/google-photos-sync-rs"
keywords = ["google-photos", "backup", "windows", "deduplication", "tray"]
categories = ["command-line-utilities", "filesystem"]

[package.metadata.winresource]
ProductName = "Google Photos Sync"
FileDescription = "Native Google Photos folder backup"
CompanyName = "Google Photos Sync Contributors"
OriginalFilename = "gphotos-sync.exe"
LegalCopyright = "Copyright (c) 2026 Google Photos Sync Contributors"

[dependencies]
reqwest = { version = "0.13.4", default-features = false, features = ["blocking", "form", "json", "native-tls", "query"] }
rusqlite = { version = "0.40.2", default-features = false, features = ["bundled"] }
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
sha2 = "0.11.0"
walkdir = "2.5.0"
windows-sys = { version = "0.61.2", features = [
    "Win32_Foundation",
    "Win32_Graphics_Gdi",
    "Win32_Security_Cryptography",
    "Win32_System_Com",
    "Win32_System_LibraryLoader",
    "Win32_System_Registry",
    "Win32_System_Threading",
    "Win32_UI_Controls",
    "Win32_UI_Controls_Dialogs",
    "Win32_UI_Input_KeyboardAndMouse",
    "Win32_UI_Shell",
    "Win32_UI_Shell_Common",
    "Win32_UI_WindowsAndMessaging",
] }

[build-dependencies]
winresource = "0.1.31"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
